{% extends 'base.html.twig' %}

{% block title %}{{ 'main_menu_user_edit' | trans }}{% endblock %}

{% block body %}

    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-lg btn-primary btn-block mt-3">{{ 'main_menu_user_edit' | trans }}</button>
        {{ form_end(form) }}
    </div>

{% endblock %}